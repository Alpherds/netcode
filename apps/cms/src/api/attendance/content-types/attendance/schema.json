{
  "kind": "collectionType",
  "collectionName": "attendances",
  "info": {
    "singularName": "attendance",
    "pluralName": "attendances",
    "displayName": "attendance"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "join_time": {
      "type": "datetime"
    },
    "leave_time": {
      "type": "datetime"
    },
    "attendance_status": {
      "type": "enumeration",
      "enum": [
        "PRESENT",
        "LATE",
        "ABSENT",
        "LEFT"
      ]
    },
    "source": {
      "type": "enumeration",
      "enum": [
        "MANUAL",
        "AUTO",
        "PROVIDER_EVENT"
      ]
    },
    "class_session": {
  "type": "relation",
  "relation": "manyToOne",
  "target": "api::class-session.class-session"
},
"student": {
  "type": "relation",
  "relation": "manyToOne",
  "target": "api::profile.profile"
}
  }
}
